<div class="container">
  <div class="row">
     <% @users.each do |user| %>
        <div class="col-12 user-name">
          <%= user.first_name %> <%= user.last_name %>
        </div>
    <% end %>
    <%= paginate @users %>
  </div>
</div>
